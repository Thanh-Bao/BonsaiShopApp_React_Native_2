import { StyleSheet } from 'react-native'

const Styles= StyleSheet.create({
    centerColumn: { flex: 1, flexDirection: "column", justifyContent: 'center' },
    centerRow: { flex: 1, flexDirection: "row", justifyContent: 'center' }
})

export default Styles 